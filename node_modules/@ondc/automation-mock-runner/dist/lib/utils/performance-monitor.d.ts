/**
 * Performance monitoring utilities for MockRunner
 */
export interface PerformanceMetric {
    operationType: "generate" | "validate" | "requirements";
    actionId: string;
    executionTime: number;
    timestamp: string;
    success: boolean;
    memoryUsage?: NodeJS.MemoryUsage;
    codeComplexity?: number;
}
export declare class PerformanceMonitor {
    private static instance;
    private metrics;
    private readonly maxMetrics;
    private constructor();
    static getInstance(): PerformanceMonitor;
    recordMetric(metric: PerformanceMetric): void;
    getMetrics(operationType?: PerformanceMetric["operationType"]): PerformanceMetric[];
    getAverageExecutionTime(operationType: PerformanceMetric["operationType"]): number;
    getSlowOperations(thresholdMs?: number): PerformanceMetric[];
    getSummary(): {
        totalOperations: number;
        averageExecutionTime: number;
        slowOperations: number;
        successRate: number;
        memoryTrend: "increasing" | "decreasing" | "stable";
    };
    clearMetrics(): void;
    createTimer(operationType: PerformanceMetric["operationType"], actionId: string): {
        finish: (success: boolean, codeComplexity?: number) => number;
    };
}
